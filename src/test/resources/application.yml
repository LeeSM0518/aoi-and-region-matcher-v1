server:
  port: 8989

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect

  datasource:
    url: jdbc:tc:postgis:14-3.2-alpine:///test-db
    username: postgres
    password: postgres